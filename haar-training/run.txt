# Create positive samples overlaying the small pic on the negative samples. 
# On the .lst file you will see x, y, w, h of the object

opencv_createsamples.exe -img .\watch5050.jpg -bg .\bg.txt -info info/info.lst -pngoutput -maxxangle 0.5 -maxyangle 0.5 -maxzangle 0.5 -num 1330

# Create the .vec file for training
opencv_createsamples.exe -info .\info\info.lst -num 1330 -w 20 -h 20 -vec positives.vec

#Training
# numStages 
# Recommended 2:1 ratio of pos/neg

opencv_traincascade.exe -data model -vec .\positives.vec -bg .\bg.txt -numPos 12
00 -numNeg 600 -numStages 5 -w 20 -h 20

